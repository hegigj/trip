import { ScoreBadgePipe } from './score-badge.pipe';

describe('ScoreBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
